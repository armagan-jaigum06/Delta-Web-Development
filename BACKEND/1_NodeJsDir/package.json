{
  "name": "nodejsdir",
  "version": "1.0.0",
  "description": "For Project we upload only package.json folder to gate the package \r select that folder in commad and type in terminal \r # npm install",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Armagan JAIGUM",
  "license": "ISC",
  "dependencies": {
    "random-words": "^2.0.1"
  }
}
